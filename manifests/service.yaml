apiVersion: v1
kind: Service
metadata:
  name: service-2048
  namespace: game-2048
spec:
  selector:
    app: app-2048
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
